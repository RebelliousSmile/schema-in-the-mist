{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/RebelliousSmile/schema-in-the-mist/v1.0.0/schemas/v1/legend-in-the-mist/story-theme.schema.json",
  "type": "object",
  "properties": {
    "title_tag": {
      "default": "Untitled Story Theme",
      "description": "The Story Theme's own tag, which names it and can be invoked like any other tag. Written without the surrounding braces of the inline tag syntax.",
      "examples": [
        "The Village I Left Behind",
        "Sworn to the Winter Court",
        "Blade of my Father"
      ],
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "level": {
      "default": "origin",
      "description": "Tier this Story Theme belongs to. Origin themes describe where a Hero comes from, Adventure themes what they have gained on the road, and Greatness themes what they have become.",
      "examples": [
        "origin",
        "adventure",
        "greatness"
      ],
      "type": "string",
      "enum": [
        "origin",
        "adventure",
        "greatness"
      ]
    },
    "category": {
      "description": "Themebook this Story Theme is built from. Kept a free string rather than a closed set, so that homebrew themebooks remain expressible.",
      "examples": [
        "Past",
        "Personality",
        "Relic",
        "Destiny",
        "Companion"
      ],
      "type": "string"
    },
    "power_tags": {
      "description": "The tags this Story Theme grants to the Hero, in the order they are written on the sheet.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S",
        "description": "One tag the Story Theme grants, which a Hero can invoke to be Favored. Written without the surrounding braces of the inline tag syntax; a renderer adds them.",
        "examples": [
          "stubborn as the hills",
          "knows every back alley"
        ]
      }
    },
    "weakness_tags": {
      "description": "The tags this Story Theme turns against the Hero, in the order they are written on the sheet.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S",
        "description": "One tag that works against the Hero, which the Narrator can invoke to Imperil them. Written without the surrounding braces and without the leading marker of the inline weakness syntax; belonging to this field is what makes it a weakness.",
        "examples": [
          "owes a debt",
          "cannot refuse a dare"
        ]
      }
    },
    "quest": {
      "description": "What the Hero is trying to settle through this Story Theme. Supports inline Markdown.",
      "examples": [
        "Return home without being recognized.",
        "Find out who ordered the burning."
      ],
      "type": "string"
    },
    "improve": {
      "description": "Number of Improve marks currently on this Story Theme's track.",
      "examples": [
        0,
        1,
        3
      ],
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    },
    "abandon": {
      "description": "Number of Abandon marks currently on this Story Theme's track.",
      "examples": [
        0,
        2
      ],
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    },
    "milestone": {
      "description": "Whether this Story Theme has reached its milestone and is ready to evolve or be replaced.",
      "examples": [
        true,
        false
      ],
      "type": "boolean"
    },
    "meta": {
      "description": "Attribution and cataloging fields for the Story Theme's origin.",
      "type": "object",
      "properties": {
        "publication_type": {
          "default": "homebrew",
          "description": "Classifies the Story Theme's source to aid cataloging and tooling.",
          "examples": [
            "official",
            "cauldron"
          ],
          "type": "string",
          "enum": [
            "official",
            "third_party",
            "cauldron",
            "homebrew"
          ]
        },
        "source": {
          "description": "Source title (book, supplement, PDF) where this Story Theme appears.",
          "examples": [
            "Legend in the Mist - Core Book Volume I - The Player",
            "Lantern in the Mist - Sample Story Themes"
          ],
          "type": "string"
        },
        "authors": {
          "description": "List of credited authors or contributors.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "pattern": "\\S",
            "description": "One credited author name.",
            "examples": [
              "Son of Oak",
              "4rtamis"
            ]
          }
        },
        "page": {
          "description": "Page number (if relevant to the source).",
          "examples": [
            64,
            88,
            231
          ],
          "type": "integer",
          "minimum": 1,
          "maximum": 9007199254740991
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false,
  "description": "Legend in the Mist - Story Theme, one of the themes a Hero is built from, holding its title tag, the tags it grants, the tags it turns against the Hero, and the Quest it carries."
}
